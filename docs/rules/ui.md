```md id="p4v8zn"
# UI Rules

## Units
- Use `rem` by default.
- Use `px` only for borders and small fixed details.

## Components
- Always reuse existing components.
- If none exist, create a reusable one.

## Colors
- Always use colors from the theme.
```